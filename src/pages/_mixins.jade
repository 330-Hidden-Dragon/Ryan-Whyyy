mixin post(title, videoType, source, description, key)
  .post&attributes(attributes)
    - var key = key || 'Issue:' + title + ':' + description.substring(0, 25).replace(/ /g, '_')
    a(href='./issue.html?' + key)
      h2.title=title
    .video
      .click-capture
      - if (videoType === 'iframe') {
        iframe(
          height="300",
          src!=source + '?enablejsapi=1',
          frameborder="0",
          allowfullscreen
          )
      - } else {
        video(data-src!=source, controls)
      - }
    a(href='./issue.html?' + key)
      p.description
        != description

mixin back-btn()
  i.fa.fa-chevron-left.back-btn.btn.column.one
